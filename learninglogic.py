import numpy as np
import tournamentlogic
import pickle
import neuronal


class learning_round(object):
    def __init__(neural_nets, number_rows, number_columns, number_contiguous, depth, single_opponent_option):
        if single_opponent_option:
            training_partner = 
            tournamentlogic.tournament_pretraining(neural_nets, )
    